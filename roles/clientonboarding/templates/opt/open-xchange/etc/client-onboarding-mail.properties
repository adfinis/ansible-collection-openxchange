{{ ansible_managed | comment }}
# The properties for mail on-boarding module

### Generic IMAP settings
################################################################################

# Specifies the IMAP host name.
# If not set, falls-back to internal settings for accessing the primary account.
#
# No default value
com.openexchange.client.onboarding.mail.imap.host={{ clientonboarding_mail_imap_host }}

# Specifies the IMAP port.
# If not set, falls-back to internal settings for accessing the primary account.
#
# No default value
com.openexchange.client.onboarding.mail.imap.port={{ clientonboarding_mail_imap_port }}

# Specifies whether a secure connection is supposed to established to access the IMAP server.
# If not set, falls-back to internal settings for accessing the primary account.
#
# No default value. Possible values: true|false
com.openexchange.client.onboarding.mail.imap.secure={{ clientonboarding_mail_imap_secure | ternary('true', 'false') }}

# Specifies whether a CustomLoginSource service should be used for the imap login name or not.
# This service can be used to provide custom imap login names.
#
# Defaults to 'false'
com.openexchange.client.onboarding.mail.imap.login.customsource=false



### Generic SMTP settings
################################################################################

# Specifies the SMTP host name.
# If not set, falls-back to internal settings for accessing the primary account.
#
# No default value
com.openexchange.client.onboarding.mail.smtp.host={{ clientonboarding_mail_smtp_host }}

# Specifies the SMTP port.
# If not set, falls-back to internal settings for accessing the primary account.
#
# No default value
com.openexchange.client.onboarding.mail.smtp.port={{ clientonboarding_mail_smtp_port }}

# Specifies whether a secure connection is supposed to established to access the SMTP server.
# If not set, falls-back to internal settings for accessing the primary account.
#
# No default value. Possible values: true|false
com.openexchange.client.onboarding.mail.smtp.secure={{ clientonboarding_mail_smtp_secure | ternary('true', 'false') }}

# Specifies whether a CustomLoginSource service should be used for the smtp login name or not.
# This service can be used to provide custom smtp login names.
#
# Defaults to 'false'
com.openexchange.client.onboarding.mail.smtp.login.customsource=false
